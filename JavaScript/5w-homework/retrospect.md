# 과제 수행 내용 및 회고
## 과제 수행 과정 기록
1. **데이터 성격 분류**
- dashboard, tabButtons, tabPanels 등 DOM에서 한 번 찾아온 요소들은 프로그램 실행 중 참조 대상이 바뀌지 않으므로 const를 사용하여 의도치 않은 재할당을 방지함.

- targetId와 같이 클릭 시점마다 새로 계산되는 값 역시 해당 블록 내에서는 변하지 않으므로 const를 적용함.

2. **명명 규칙 수립**
- 변수명은 역할을 직관적으로 알 수 있도록 복수형(tabButtons)과 단수형(button)을 구분하여 명명함.

3. **가독성 최적화**
- 중첩된 for 문 대신 forEach 메서드를 사용하여 배열 요소에 직접 접근함으로써 코드의 길이를 줄이고 논리 흐름을 개선함.

- 아직 배우지 않은 getAttribute, setAttribute를 사용해 data-*속성을 가져와 사용.

4. **실행 및 검증**
- console.log를 활용해 클릭된 요소의 targetId와 인덱스가 정확히 매칭되는지 콘솔 창에서 실시간으로 검증함.

---
## 테스트 코드 작성
- 각 로직이 의도대로 동작하는지 확인하기 위해 모든 변수를 `console.log()`로 직접 출력하여 검증.

- Tab 전환 테스트: 버튼 클릭 시 tab-panel이 정상적으로 연결됐는지 콘솔로 확인.

---
## 과제 코드 리뷰
- **상태 관리의 일관성**: 새로운 탭을 활성화하기 전, 기존의 모든 상태를 forEach로 일괄 초기화하는 방식은 상태 꼬임 문제를 방지하는 가장 안전한 방법임.

---
## 회고(깨달은 점, 아쉬운 점 등)
이번에는 도저히 처음부터 짤 자신이 없어서 강사님이 올려주신 base 파일로 자바스크립트 로직만 짜게 됐다. 그래도 과제용은 만들었으니, 이제 자고 일어나서 다시 처음부터 만들기를 시도는 해볼 것이다. 비록 AI의 도움은 좀 받겠지만...
이번주는 내용이 재밌지만 어려워서 그런지 집중이 너무 안됐던 것 같다. 과제할때도 그렇고..ㅠㅠ 하지만 칼을 뽑았으니 무라도 썰어야하지않겠는가..!! 평일에 필기하느라 미뤘던 복습공부를 다시 해봐야겠다. 